apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# db.yaml 不在 base：production / dev 各自維護獨立 DB，
# 放在各 overlay 的 resources 中，namespace 由 overlay 自動套用。
resources:
  - env.yaml
  - app.yaml
  - worker.yaml
  - podmonitor.yaml
